# Copy to .env and fill in. .env is gitignored — never commit the real key.

# Pioneer API key, used by the distillation worker with --extractor pioneer.
# Get one at https://agent.pioneer.ai (Pro plan / hackathon promo).
PIONEER_API_KEY=your-pioneer-key-here
